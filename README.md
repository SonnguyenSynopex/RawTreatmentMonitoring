# RawTreatmentMonitoring

Frontend **Raw Treatment & UF** (từ dự án `raw`) — dashboard SVG realtime + Record.

| | |
|--|--|
| Dashboard | `index.html` |
| Record | `record.html` |
| Docs UI | `RAWdoc.md` |
| MQTT bridge | `BE/` |
| MQTT contract | `MQTT_HIVEMQ_BACKEND_SPEC.md` |

Deploy Vercel: static root (`index.html`). Realtime trên Vercel cần MQTT (HiveMQ WSS) — xem BE + MQTT spec.
